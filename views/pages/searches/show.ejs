<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta
      name="viewport"
      content="width=device-width, 
  initial-scale=1.0"
    />
    <link rel="stylesheet" href="/styles/reset.css" />
    <link rel="stylesheet" href="/styles/base.css" />
    <link rel="stylesheet" href="/styles/layout.css" />
    <link rel="stylesheet" href="/styles/modules.css" />
    <title>Book App</title>
  </head>
  <body>
    <h2>Search Results</h2>
    <div class="Results">
      <% for (let i=0; i < bookArr.length; i++) { %>
      <form action="/books/added" method="POST">
        <fieldset>
          <input type="hidden" name="title" value="<%= bookArr[i].title %>">
          <input type="hidden" name="author" value="<%= bookArr[i].author %>">
          <input type="hidden" name="description" value="<%= bookArr[i].description %>">
          <input type="hidden" name="image" value="<%= bookArr[i].image %>">
          <input type="submit" />
          <legend>Add a Book</legend>
          <label for="">Add Book</label>
        </fieldset>
      </form>
      <li><h3 class="title"><%= bookArr[i].title %></h3></li>
      <li><p class="author"><%= bookArr[i].author %></p></li>
      <li><p class="description"><%= bookArr[i].description %></p></li>
      <li></li>
      <li><img src="<%= bookArr[i].image %>" alt=""></li>
    </div>
    <% } %>
  </body>
</html>
