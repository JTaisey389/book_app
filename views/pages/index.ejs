<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/reset.css" />
    <link rel="stylesheet" href="/styles/base.css" />
    <link rel="stylesheet" href="/styles/layout.css" />
    <link rel="stylesheet" href="/styles/modules.css" />
    <title>Book App</title>
  </head>
  <body>
    <header>
      <h1>All About Books!</h1>
      <nav>
        <h3><a href="/">Home</a></h3>
        <h3><a href="/searches/new">Search for Books</a></h3>
      </nav>
    </header>
    <main>
      <div class="my-collection">
        <h2><%= allBooks.length %> My Saved Books</h2>
        <% for (let i = 0; i < allBooks.length; i++) { %>
        <ul>
          <li><img src="<%= allBooks[i].image_url %>" /></li>
          <li><%= allBooks[i].title %></li>
          <li><%= allBooks[i].author %></li>
        </ul>
        <a href="/books/<%= allBooks[i].id %>"><button>View Details</button></a>
        <% } %>
      </div>
    </main>
  </body>
</html>