<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book App</title>
</head>
<body>
  Show Books Page
  <form action="/server.js" method="POST">
    <fieldset>
      <legend>Add a Book</legend>
      <label for="">Add Book</label>
      <input type="submit">
    </fieldset>
  </form>
    <ul>
      <% for (let i = 0; i < allBooks.length; i++){ %>
      <li>
        <h2><%= allBooks[i].name %></h2>
        <p>Title: <%= allBooks[i].title_name %></p>
          <a href="/books/<%= allBooks[i].id %>"><button>View Details</button></a>
      </li>
     <% } %>
    </ul>
    <!-- LOOP here for the BOOKs object -->
  
</body>
</html>