<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book App</title>
  </head>
  <body>
    Show Books Page
    <p>Total number of books in my collection <%= count %></p>
    <ul>
      <% for (let i = 0; i < allBooks.length; i++){ %>
      <li><h3 class="title"><%= allBooks[i].title %></h3></li>
      <li><p class="author"><%= allBooks[i].author %></p></li>
      <li><img src="<%= allBooks[i].image %>" alt="" /></li>
      <a href="/books/<%= allBooks[i].id %>"><button>View Details</button></a>
      <% } %>
    </ul>
  </body>
</html>
